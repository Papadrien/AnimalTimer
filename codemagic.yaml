workflows:
  flutter-app:
    name: Animal Timer Build
    environment:
      flutter: 3.22.3
    scripts:
      - flutter clean
      - flutter create .
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
